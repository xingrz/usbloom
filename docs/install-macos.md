# Install USBloom on macOS

Requires macOS 12 or later on Apple Silicon.

Drag USBloom into Applications, then eject the disk image.
If macOS blocks the app from opening, run:

```sh
xattr -dr com.apple.quarantine /Applications/USBloom.app
```

Open USBloom from Applications.
